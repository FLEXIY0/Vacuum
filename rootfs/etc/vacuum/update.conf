# Vacuum — where vacuum-update pulls from.
#
# Point these at your own fork and branch to make an installed machine
# follow your tree instead of upstream.

VACUUM_REPO="FLEXIY0/Vacuum"
VACUUM_BRANCH="claude/vacuum-os-build-tndaq3"
