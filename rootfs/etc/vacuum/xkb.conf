# Vacuum — keyboard layouts, read by ~/.xinitrc.
#
# The live image ships US + Russian with Alt+Shift to switch. Set a single
# layout by dropping the comma, e.g. VACUUM_XKB_LAYOUT=us.
VACUUM_XKB_LAYOUT="us,ru"
VACUUM_XKB_OPTIONS="grp:alt_shift_toggle"
